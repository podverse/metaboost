# Deprecated Legacy SQL Path

SQL is no longer maintained in `infra/management-database/`.

Canonical management schema SQL now lives in:

- `infra/k8s/base/db/postgres-init/0005_management_schema.sql.frag`

This directory is kept only as a deprecated placeholder to avoid stale path assumptions.

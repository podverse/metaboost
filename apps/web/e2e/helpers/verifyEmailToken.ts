/**
 * Raw token for verify-email E2E. Must match the token seeded in tools/web/seed-e2e.mjs
 * (hash stored in verification_token for E2E user e2e-bucket-owner@example.com).
 */
export const E2E_VERIFY_EMAIL_TOKEN_RAW = 'e2e2' + '0'.repeat(28);

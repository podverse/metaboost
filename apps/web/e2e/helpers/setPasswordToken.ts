/**
 * Raw token for set-password E2E (short to avoid URL truncation). Must match the token seeded in tools/web/seed-e2e.mjs
 * (hash stored in verification_token for E2E invite user e2e-invite@example.com).
 */
export const E2E_SET_PASSWORD_TOKEN_RAW = 'e2e1' + '0'.repeat(28);

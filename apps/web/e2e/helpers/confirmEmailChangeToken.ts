/**
 * Raw token for confirm-email-change E2E. Must match the token seeded in tools/web/seed-e2e.mjs
 * (hash stored in verification_token for E2E user e2e-bucket-owner@example.com, payload pending_email).
 */
export const E2E_CONFIRM_EMAIL_CHANGE_TOKEN_RAW = 'e2e3' + '0'.repeat(28);

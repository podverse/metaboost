/** Cookie name for persisting table sort preferences (sortBy/sortOrder per list). */
export const TABLE_SORT_PREFS_COOKIE_NAME = 'table_sort_prefs';

/** Cookie name for persisting Bucket Summary range/view preferences by path. */
export const BUCKET_SUMMARY_PREFS_COOKIE_NAME = 'bucket_summary_prefs';

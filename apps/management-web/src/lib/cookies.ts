/** Cookie name for persisting table sort preferences (sortBy/sortOrder per list). */
export const TABLE_SORT_PREFS_COOKIE_NAME = 'management_table_sort_prefs';

/** Cookie name for persisting table search / filter columns / page / events timeline sort per list key. */
export const TABLE_LIST_STATE_COOKIE_NAME = 'management_table_list_state';

/** Cookie name for bucket detail pathname-keyed tab and messages page. */
export const BUCKET_DETAIL_NAV_COOKIE_NAME = 'management_bucket_detail_nav';

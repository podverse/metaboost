/** Number of days until a bucket-admin invitation link expires. */
export const BUCKET_ADMIN_INVITATION_EXPIRY_DAYS = 7;

/** Byte length of the random token for bucket-admin invitation links. */
export const BUCKET_ADMIN_INVITATION_TOKEN_BYTES = 32;

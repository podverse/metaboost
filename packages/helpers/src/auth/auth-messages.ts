/**
 * Canonical auth result messages. Use these in APIs when returning error messages
 * and in web apps when comparing result.message so API and client stay aligned.
 * Apps can map these to i18n keys for display.
 */
export const AUTH_MESSAGE_LOGIN_FAILED = 'Login failed';
export const AUTH_MESSAGE_INVALID_CREDENTIALS = 'Invalid credentials';

/**
 * Debounce delay (ms) for search/filter input before syncing to URL or triggering request.
 * Use with setTimeout when the user types so we don't update on every keystroke.
 */
export const SEARCH_DEBOUNCE_MS = 300;

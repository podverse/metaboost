---
description: Require semicolons in JS/TS files
globs: **/*.{js,jsx,ts,tsx}
alwaysApply: false
---

# Semicolons Required

Use semicolons at the end of statements in JavaScript and TypeScript (`.js`, `.jsx`, `.ts`, `.tsx`). Markdown and `.mdc` files are exempt. Keep semicolons consistent in existing code.

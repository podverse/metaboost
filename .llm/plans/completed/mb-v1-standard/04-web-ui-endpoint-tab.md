# Web UI

- `BucketForm`: third tile Custom (`mb-root`).
- `routes.ts`: `BucketDetailTab` includes `endpoint`.
- Bucket detail page: Endpoint tab + `EndpointPanel` for `mb-*`; Add to RSS only for `rss-channel`.
- `env.ts`: `getMbV1BoostPublicUrl`, `getMbV1OpenApiSpecUrl`.
- Child: new form/route for mb children only from `mb-root` / `mb-mid`.

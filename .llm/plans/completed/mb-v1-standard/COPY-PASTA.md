# Copy-pasta prompts

## Implement schema + ORM (Phase 1)

Implement `01-schema-migrations-orm.md` in the Metaboost repo: migration `0008_mb_bucket_types.sql`, ORM bucket types, BucketService rules, helpers-requests unions.

## Implement mb-v1 API (Phase 2)

Implement `02-api-standard-mb-v1-and-refactor.md`: shared standard ingest lib, mb-v1 controller/routes/OpenAPI, wire `standardEndpoint` and api-docs; refactor mbrss-v1 to share code.

## Implement bucket policy (Phase 3)

Implement `03-buckets-child-create-and-policy.md`: create/child schemas and controller validation.

## Implement web (Phase 4)

Implement `04-web-ui-endpoint-tab.md`.

## Tests (Phase 5)

Implement `05-tests-e2e-management.md`.

## Podverse (Phase 6)

Implement `06-podverse-v4v-metaboost.md` in the Podverse monorepo.

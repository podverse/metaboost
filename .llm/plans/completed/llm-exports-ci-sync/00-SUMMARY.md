# LLM exports CI sync — summary (implemented)

Parity with Podverse: see the Podverse file at `podverse/.llm/plans/completed/llm-exports-ci-sync/00-SUMMARY.md` for the full description. This repo includes the same `scripts/llm/`, `.llm/exports/`, workflows, and documentation patterns under Metaboost paths.

## Verification

- From repo root: `npm run llm:exports:sync` and `git add .llm/exports && npm run llm:exports:check` → exit 0

# metaboost-type-fixes-orm-dist-membership

Started: 2026-05-04

### Session 1 - 2026-05-04

#### Prompt (Developer)

Metaboost type fixes (ORM dist + MembershipTier)

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

#### Key Decisions

- Rebuilt `packages/orm` so `dist/types/UserWithRelations.d.ts` includes `trustSettings` (fixes management-api consumer types).
- Mapped API `membershipTier` string (`'trial' | 'premium' | null`) to `MembershipTier` enum on the user edit page via explicit branch (`'premium'` → `Premium`, else `Trial`).

#### Files Modified

- `packages/orm/dist/**` (regenerated by `tsc`)
- `apps/management-web/src/app/(main)/user/[id]/edit/page.tsx`

### Session 2 - 2026-05-04

#### Prompt (Developer)

For the code present, we get this error:

```
Type 'string' is not assignable to type 'MembershipTier | null`.
```

Fix it, verify, and then give a concise explanation. @metaboost/apps/management-api/src/controllers/usersController.ts:52-55

#### Key Decisions

- Added `membershipTierFromStoredValue` in `@metaboost/helpers` to map ORM/DB `string` to `MembershipTier | null` without type assertions.
- Used it in `userToJson`, `createUser` (default trial), and `updateUser` / `upsertTrustSettings` body mapping; refactored user edit page to call the same helper.
- Replaced `MembershipTier.PaidPremium` comparison with `MembershipTier.Premium` in create default trust tier logic for consistent enum narrowing.

#### Files Created/Modified

- `packages/helpers/src/trust/membershipTierFromStoredValue.ts`
- `packages/helpers/src/trust/membershipTierFromStoredValue.test.ts`
- `packages/helpers/src/index.ts`
- `packages/helpers/dist/**` (via `npm run build -w packages/helpers`)
- `apps/management-api/src/controllers/usersController.ts`
- `apps/management-web/src/app/(main)/user/[id]/edit/page.tsx`

### Session 3 - 2026-05-04

#### Prompt (Developer)

For the code present, we get this error:

```
This comparison appears to be unintentional because the types '"free_trial" | "paid_premium" | null' and 'MembershipTier.Trial' have no overlap.
```

Fix it, verify, and then give a concise explanation. @metaboost/apps/management-web/src/app/(main)/user/[id]/page.tsx:63-66

#### Key Decisions

- Resolved API `membershipTier` with `membershipTierFromStoredValue`, then compared to `MembershipTier.Trial` for the status label and trial-only note (avoids comparing API string unions to enum members directly).

#### Files Modified

- `apps/management-web/src/app/(main)/user/[id]/page.tsx`

### Session 4 - 2026-05-04

#### Prompt (Developer)

For the code present, we get this error:

```
Type 'MembershipTier' is not assignable to type '"free_trial" | "paid_premium" | undefined'.
```

Fix it, verify, and then give a concise explanation. @metaboost/apps/management-web/src/components/users/UserForm.tsx:194-197

#### Key Decisions

- Added `membershipTierToApiBodyValue` in `@metaboost/helpers` mapping `MembershipTier` → `'trial' | 'premium'` for management-api JSON bodies; used in `UserForm` create and update payloads.
- Rebuilt `@metaboost/helpers-requests` so `dist/` matched `src` (`trial`/`premium`); stale `dist` had `free_trial`/`paid_premium`, which caused TS mismatches with the API and helpers.

#### Files Created/Modified

- `packages/helpers/src/trust/membershipTierToApiBodyValue.ts`
- `packages/helpers/src/trust/membershipTierToApiBodyValue.test.ts`
- `packages/helpers/src/index.ts`
- `packages/helpers/dist/**` (via build)
- `packages/helpers-requests/dist/**` (via build)
- `apps/management-web/src/components/users/UserForm.tsx`

### Session 5 - 2026-05-04

#### Prompt (Developer)

For the code present, we get this error:

```
Duplicate enum member value premium.
```

Fix it, verify, and then give a concise explanation. @metaboost/packages/helpers/src/trust/constants.ts:16-19

#### Key Decisions

- Removed `MembershipTier.FreeTrial` and `MembershipTier.PaidPremium` (duplicate string values of `Trial` / `Premium`); updated `UserForm` and `UserService` to use `MembershipTier.Trial` and `MembershipTier.Premium` only.

#### Files Modified

- `packages/helpers/src/trust/constants.ts`
- `packages/helpers/src/trust/membershipTierToApiBodyValue.test.ts`
- `packages/orm/src/services/UserService.ts`
- `apps/management-web/src/components/users/UserForm.tsx`
- `packages/helpers/dist/**` (via build)
- `packages/orm/dist/**` (via build)

### Session 6 - 2026-05-04

#### Prompt (Developer)

@metaboost/packages/orm/src/services/UserService.ts:13-14 this seems like it should be configurable by env vars

@metaboost/packages/orm/src/services/UserService.ts:16-20 this seems like it should be imported from a reusable helper package somewhere

#### Key Decisions

- Added `addMonths` in `@metaboost/helpers` (`time/addMonths`) and `membershipDefaultTrialMonths` / `membershipDefaultPremiumMonths` in ORM reading `MEMBERSHIP_DEFAULT_TRIAL_MONTHS` and `MEMBERSHIP_DEFAULT_PREMIUM_MONTHS` (1–120; empty → 3 / 12).
- Wired optional startup validation in `apps/api` and `apps/management-api`; documented keys in env templates, `apps/api/.env.example`, and K8s base `api` / `management-api` env files.

#### Files Created/Modified

- `packages/helpers/src/time/addMonths.ts`, `addMonths.test.ts`, `index.ts`, `dist/**`
- `packages/orm/src/lib/membershipDefaultMonths.ts`, `UserService.ts`, `dist/**`
- `apps/api/src/lib/startup/validation.ts`, `apps/management-api/src/lib/startup/validation.ts`
- `infra/config/env-templates/api.env.example`, `management-api.env.example`
- `apps/api/.env.example`
- `infra/k8s/base/api/source/api.env`, `infra/k8s/base/management-api/source/management-api.env`

### Session 7 - 2026-05-04

#### Prompt (Developer)

@podverse/apps/api/src/controllers/account/accountAddByRSSParse.ts:228 make the requireCapability key value in podverse and metaboost an importable constant

#### Key Decisions

- Metaboost: added `ACCOUNT_ENTITLEMENT_CAPABILITY` and `AccountEntitlementCapability` in `packages/helpers/src/trust/accountEntitlementCapability.ts` (Podverse-aligned strings).

#### Files Created/Modified

- `packages/helpers/src/trust/accountEntitlementCapability.ts`
- `packages/helpers/src/index.ts`
- `packages/helpers/dist/**` (via `tsc`)
